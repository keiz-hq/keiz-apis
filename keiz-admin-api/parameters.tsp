using Http;

namespace Parameters;

model carId {
  @path carId: uuid;
}
model imageId {
  @path imageId: uuid;
}
model priceId {
  @path priceId: uuid;
}
model attributeId {
  @path attributeId: uuid;
}
model choiceId {
  @path choiceId: uuid;
}
